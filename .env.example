# Example .env - copy to .env and edit values
JWT_SECRET=غيّر_هذا_السر_القوي
# Firebase: either place serviceAccountKey.json in project root
# or set GOOGLE_APPLICATION_CREDENTIALS to its path.
# SMTP (optional)
SMTP_HOST=smtp.example.com
SMTP_PORT=465
SMTP_USER=your_email@example.com
SMTP_PASS=your_smtp_password
